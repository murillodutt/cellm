---
description: Interactive Oracle setup — install, status, update, diagnostics, uninstall
argument-hint: "[install|status|update|doctor|restart|uninstall]"
---

Manage Oracle worker daemon. Full instructions in the **init** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as action (default: interactive menu).
