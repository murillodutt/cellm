---
description: CELLM OS command center — list specs, create checks, view status
argument-hint: "[action]: (empty) | create <title> | status | treat <check>"
---

Entry point for the atomic spec-driven system. Full instructions in the **spec** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as action. Use `treat <check>` to work through a specific check.
