---
description: Run ARENA quality checks — tests, typecheck, health verification with trend reporting
argument-hint: "[scope]: all | labs | typecheck | health | file <path>"
---

Run ARENA quality checks. Full instructions in the **arena** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as scope (default: `all`).
