---
description: Scan project to extract design system signals and bootstrap DSE
argument-hint: "[project-path]"
---

Detect framework, colors, typography, and components. Full instructions in the **dse-discover** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as project path (default: current directory).
