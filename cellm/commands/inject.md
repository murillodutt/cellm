---
description: Inject relevant patterns into the current context
argument-hint: "[pattern-path...]"
---

Analyze work context and suggest matching patterns. Full instructions in the **inject** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as specific pattern paths, or omit for auto-detection.
