---
description: Bootstrap a clean documentation structure with LLM-first templates
argument-hint: "[docRoot]"
---

Initialize documentation structure. Full instructions in the **init** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as `docRoot` (default: `docs/technical`).
