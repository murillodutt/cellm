---
description: Restore an archived document back to active documentation
argument-hint: "<file> [--reason \"text\"]"
---

Restore an archived document. Full instructions in the **lifecycle** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as archived file path and reason.
