---
description: Archive or remove deprecated documentation and broken references
argument-hint: "[docRoot] [--dry-run]"
---

Prune deprecated and orphaned docs. Full instructions in the **prune** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as `docRoot` and options. Always preview with `--dry-run` first.
