---
description: Check and update evidence freshness status across all code-evidence files
argument-hint: "[docRoot] [--update]"
---

Validate evidence freshness. Full instructions in the **freshness** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as `docRoot` and options.
