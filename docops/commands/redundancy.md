---
description: Detect redundant and duplicate content across documentation files
argument-hint: "[docRoot] [--threshold N]"
---

Detect documentation redundancy. Full instructions in the **redundancy** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as `docRoot` and options.
