---
description: Garbage collection for resolved gaps, stale evidence, and redundant content
argument-hint: "[docRoot] [--dry-run]"
---

Run documentation garbage collection. Full instructions in the **gc** skill — loaded automatically when this command runs.

Pass `$ARGUMENTS` as `docRoot` and options. Always preview with `--dry-run` first.
