# CELLM Oracle Environment Configuration
# Copy this file to .env and adjust values as needed

# Path to the CELLM project root (relative to oracle/ directory)
# Default: ".." (parent directory containing CLAUDE.md and .claude/)
CELLM_CORE_PATH=..

# Node environment
NODE_ENV=development

# Server port (optional, defaults to 3000)
# NUXT_PORT=3000

# Server host (optional, defaults to localhost)
# NUXT_HOST=localhost
