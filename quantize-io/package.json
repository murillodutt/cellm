{
  "name": "@quantize-io/monorepo",
  "private": true,
  "version": "0.36.66",
  "description": "Quantize-IO (QT) \u2014 5th CELLM family plugin. Token I/O quantization for LLM hosts.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build && bun scripts/build-adapter-bundle.ts",
    "embed:rules": "bun scripts/embed-rules.ts",
    "typecheck": "tsc --build && bun run --filter='@quantize-io/core' typecheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "6.0.2",
    "vitest": "4.1.4"
  },
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=20"
  }
}
